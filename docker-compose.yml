version: '3.9'

networks:
  whisper-net:               # external = already exists
    external: true

services:
  whisper:
    build:
      context: .
      dockerfile: Dockerfile
    image: whisper-worker-image
    container_name: whisper-python-worker
    networks:
      - whisper-net          # ← attach!
    environment:
      REDIS_HOST: redis_server   # or just 'redis' if you use that alias
      REDIS_PORT: 6379
    runtime: nvidia
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [ gpu ]
    volumes:
      - ./models:/root/.cache/whisper
      - /transcripts:/transcripts
      - /raw:/raw
      - .:/app
      - /var/run/docker.sock:/var/run/docker.sock # Mount Docker socket
      - ./supervisord.conf:/etc/supervisor/conf.d/supervisord.conf
      - type: bind
        source: ./supervisor-logs
        target: /app/logs
    env_file:
      - .env
    stdin_open: true
    tty: true
    restart: always
    command: sh -c "rm -rf /app/*.log && supervisord -c /etc/supervisor/conf.d/supervisord.conf -n"

